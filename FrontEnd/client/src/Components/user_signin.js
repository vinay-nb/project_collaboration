import styled from 'styled-components';

const Signin = (props) => {
    return(
        <Container>
            <h1>user signin</h1>
        </Container>
    )
}

const Container = styled.div`
    padding:8px;
`;

export default Signin;